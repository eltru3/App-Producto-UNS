<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Checked -->
    <item android:state_checked="true" android:color="@color/gold_strong"/>

    <!-- Pressed -->
    <item android:state_pressed="true" android:color="@color/gold_soft"/>

    <!-- Normal -->
    <item android:color="@color/colorTextUniversalInverse"/>

</selector>
